# Revisione di implementazione — US-007

Branch: `feature/US-007-vista-i-miei-step` · diff verso `develop`
Suite: 461 test, 1325 asserzioni, verdi · Pint verde · Larastan livello 5 a zero errori · `composer audit` senza advisory
Sei commit, uno per task; nulla su `develop` o `main`.

## Robert (code review)

- [High] resources/views/components/my-steps/⚡index.blade.php — N+1 nel blocco delle release in attesa: `activeStep` era caricato con `withActivationInstant()` e `assignedUser` ma **non** con la propria `release`. Quando lo step attivo e il primo della catena — cioe su ogni release appena avviata — la sottoquery torna `null` e `activationInstant()` ripiega su `release->started_at`, caricando la release una query per riga. E esattamente il criterio di accettazione "nessuna query N+1, verificato".
- [Medium] tests/Feature/MySteps/MyStepsQueryBudgetTest.php — il test di budget non poteva cogliere il rilievo precedente: le fixture chiudevano **sempre** lo step 1, quindi il ramo di ripiego non veniva mai misurato. Il "verificato" del criterio valeva solo sul percorso gia sicuro.
- [Medium] tests/Feature/MySteps/MyStepsScreenTest.php — nessuna asserzione sull'**ordinamento** dei due blocchi, benche l'ordinamento in PHP sia la decisione piu argomentata del componente: togliendo il `sortBy` la suite restava verde.
- [Medium] tests/Feature/MySteps/MyStepsScreenTest.php — `assertDontSee` vacui in due test: l'elenco della persona era vuoto, quindi le asserzioni sarebbero passate anche se la schermata non avesse reso nulla, cioe con la query rotta invece che filtrata bene.
- [Medium] app/Models/ReleaseStep.php — il ripiego di `activationInstant()` non distingue "nessuno step precedente" da "precedente esistente ma senza `completed_at`": entrambi tornano `null` e ripiegano su `started_at`. La guardia e a senso unico.
- [Low] app/Models/ReleaseStep.php — `withActivationInstant()` apre con `select('release_steps.*')`, che azzera qualunque select applicata prima nella catena; l'ordine di applicazione era vincolante ma non documentato. Nome della tabella scritto a mano in due punti invece che derivato dal modello.
- [Low] resources/views/components/my-steps/⚡index.blade.php — il commento su `aria-live="polite"` motivava la regione con aggiornamenti Livewire che oggi non avvengono: il componente non ha azioni ne polling, quindi non si ri-renderizza mai.
- [Low] resources/views/components/my-steps/⚡index.blade.php — la motivazione dell'ordinamento in PHP era piu forte del vero: `ORDER BY COALESCE(...)` e SQL standard e portabile, quindi la divergenza sui `NULL` era evitabile, non imposta.
- [Low] app/Models/Release.php e componente — `involving()` e il `whereDoesntHave('steps', ...)` costruiscono l'EXISTS dalla relazione `steps()`, che porta `->orderBy('position')`: l'ORDER BY finisce nelle sottoquery di esistenza dove e puro rumore.
- [Low] resources/views/components/my-steps/⚡index.blade.php — il filtro difensivo faceva sparire una release incoerente senza traccia: su uno strumento che esiste perche nulla resti fermo in silenzio, e il difetto peggiore possibile.

## Lars (security)

Nessun rilievo Critical, High o Medium: il perimetro di accesso della schermata regge.

- [Low] README.md e .ai/rules/routes.md — la documentazione dichiarava `/step/{step}` come *l'unica* rotta senza `->can()`; con `/` diventano due. L'omissione su `home` e corretta nel merito (nessuna ability da valutare, `auth` e la sola precondizione), ma proprio quella regola dice che senza il commento l'assenza verrebbe letta come dimenticanza.
- [Low] resources/views/components/my-steps/⚡index.blade.php — stesso N+1 latente rilevato da Robert; impatto di sicurezza marginale (consumo di risorse sulla pagina di ingresso, non divulgazione), ma difetto reale mascherato dal test.
- [Low] perimetro repo — `SECURITY.md` e `public/.well-known/security.txt` assenti. Confermato **fuori perimetro** per questa spec: strumento interno interamente dietro `auth`, nessun canale di segnalazione esterno da pubblicare. Semmai da valutare in `larapilot-ship`.

**Conforme e verificato:** blocco 1 filtrato su `assigned_user_id` + stato attivo + release in corso, nessuna riga altrui vi entra nemmeno per un amministratore; l'esposizione del nome di un collega nel blocco 2 e delimitata da `Release::involving()`, che richiede al viewer almeno uno step assegnato **su quella release**; `ReleaseStepPolicy` e `authorizeOrRecord()` immutati dal diff, e il link nella card non e mai trattato come autorizzazione; il componente non ha **alcuna proprieta pubblica**, quindi nessuno stato manipolabile dal client, e i metodi `#[Computed]` non sono invocabili come azione Livewire; `withActivationInstant()` non contiene input — nome tabella, alias e `whereColumn` sono letterali, nessun `DB::raw`/`whereRaw`; nessun `{!! !!}` nel template, ogni stringa d'utente passa da `{{ }}`; esposti solo nome e ruolo congelato, nessuna email e nessun identificativo interno; zero modifiche a `composer.json`/`composer.lock`/`package.json`; nessuna lettura di `step_definitions` o `field_definitions` nei nuovi percorsi; `role_name` congelato ovunque, con test di regressione.

## Parent actions

Fixed:

- `chaperone()` su `Release::activeStep()`: la relazione inversa arriva gia popolata, quindi il ripiego su `started_at` non ricarica la release. Costo zero query aggiuntive, contro una per riga della soluzione alternativa (`with('release')` annidato).
- Fixture del budget riscritte per **alternare le due forme**: release appena avviata ferma sul primo step (ramo di ripiego) e release gia avanzata (ramo con precedente). Verificato che rimuovendo `chaperone()` il test fallisce dicendo 5 query contro 9.
- Due nuovi test di ordinamento — `test_the_oldest_open_step_comes_first` e `test_the_release_stuck_the_longest_comes_first_among_those_waiting` — che confrontano la posizione nel corpo della risposta. Verificato che rimuovendo i due `sortBy` falliscono entrambi.
- I due `assertDontSee` vacui ora convivono con uno step **visibile** della stessa persona: l'asserzione distingue "filtrato bene" da "query rotta". Aggiunta anche l'esclusione del nome del progetto altrui.
- `withActivationInstant()` deriva il nome della tabella da `$query->getModel()->getTable()`, e il PHPDoc dichiara che lo scope **ridefinisce la select** e va applicato prima di eventuali `withCount`.
- PHPDoc di `activationInstant()`: la guardia a senso unico e ora dichiarata, con il motivo per cui distinguere i due casi costerebbe una seconda sottoquery per un dato che nessun percorso applicativo produce.
- Commento su `aria-live` riscritto per dire il vero: oggi la regione non ha nulla da annunciare, e il contratto dichiarato dal mockup per quando la pagina si aggiornera senza ricarica, e va messa prima perche una regione live che compare col proprio contenuto non annuncia comunque niente.
- Motivazione dell'ordinamento in PHP riscritta nel componente **e** nel README: `COALESCE` e portabile, la scelta si regge sul fatto che l'insieme e gia caricato ed e quello di una persona sola — con il limite dichiarato che i due blocchi non sono paginabili in database.
- Il filtro difensivo sulla release senza step attivo scrive ora un `Log::warning` con l'identificativo prima di escluderla.
- README (vincolo permanente 12) e `.ai/rules/routes.md` allineati: `/step/{step}` resta l'unica **deroga**, e `/` e distinta come rotta senza ability da valutare.

Open (Low):

- ORDER BY della relazione `steps()` che filtra dentro le sottoquery EXISTS di `involving()` e `whereDoesntHave`. Rumore nella query, nessun effetto sul risultato: neutralizzarlo con `reorder()` renderebbe le due closure meno leggibili di quanto valga il guadagno.
- `SECURITY.md` / `security.txt` assenti: preesistente e fuori perimetro, da riprendere in `larapilot-ship`.

## Consegna manuale ad Anne

Non automatizzabile sotto `settings.testing: NORMAL`, da verificare in revisione umana:

- Resa a **375 / 768 / 1024 / 1280 px** in tema chiaro **e** scuro; nessuno scroll orizzontale a nessun breakpoint.
- Bersaglio della card ≥ 44x44 px sotto 1024 px; apertura e chiusura del drawer da tastiera con `Esc` e ritorno del focus sul trigger.
- Annuncio del contatore con uno screen reader.
- Rotazione in landscape su telefono (circa 667x375): il layout deve restare a colonna singola.

**Nota d'ambiente:** finche US-011 non e consegnata, `migrate:fresh --seed` produce un ambiente **senza release** e la schermata mostra lo stato vuoto — ed e corretto. Per provarla si avvia una release da `/progetti/{progetto}/rilascio`.
